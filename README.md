# filesystem_autoindex

Recreates the nginx autoindex functionality  \
Basic directory and file listing \
First run `Make` \
then do `./autoindex`

to get the listing of other places you would have to move the binary
